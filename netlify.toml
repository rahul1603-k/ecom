[build]
  publish = "Frontend"
  command = "echo 'Static site: no build step'"

# Optional: set basic headers
[[headers]]
  for = "/*"
  [headers.values]
    X-Frame-Options = "SAMEORIGIN"
    X-Content-Type-Options = "nosniff"
